// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include <comppi/entity/gen/ProteinNameMap-odb.hxx>

#include <cassert>
#include <cstring>  // std::memcpy


#include <odb/mysql/traits.hxx>
#include <odb/mysql/database.hxx>
#include <odb/mysql/transaction.hxx>
#include <odb/mysql/connection.hxx>
#include <odb/mysql/statement.hxx>
#include <odb/mysql/statement-cache.hxx>
#include <odb/mysql/simple-object-statements.hxx>
#include <odb/mysql/container-statements.hxx>
#include <odb/mysql/exceptions.hxx>
#include <odb/mysql/simple-object-result.hxx>
#include <odb/mysql/enum.hxx>

namespace odb
{
  // ProteinNameMap
  //

  access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::id_type
  access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  id (const image_type& i)
  {
    mysql::database* db (0);
    ODB_POTENTIALLY_UNUSED (db);

    id_type id;
    {
      mysql::value_traits<
          unsigned int,
          mysql::id_ulong >::set_value (
        id,
        i._id_value,
        i._id_null);
    }

    return id;
  }

  bool access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  grow (image_type& i, my_bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // _id
    //
    t[0UL] = 0;

    // _specieId
    //
    t[1UL] = 0;

    // _namingConventionA
    //
    if (t[2UL])
    {
      i._namingConventionA_value.capacity (i._namingConventionA_size);
      grew = true;
    }

    // _proteinNameA
    //
    if (t[3UL])
    {
      i._proteinNameA_value.capacity (i._proteinNameA_size);
      grew = true;
    }

    // _namingConventionB
    //
    if (t[4UL])
    {
      i._namingConventionB_value.capacity (i._namingConventionB_size);
      grew = true;
    }

    // _proteinNameB
    //
    if (t[5UL])
    {
      i._proteinNameB_value.capacity (i._proteinNameB_size);
      grew = true;
    }

    return grew;
  }

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  bind (MYSQL_BIND* b,
        image_type& i,
        mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    std::size_t n (0);

    // _id
    //
    if (sk != statement_update)
    {
      b[n].buffer_type = MYSQL_TYPE_LONG;
      b[n].is_unsigned = 1;
      b[n].buffer = &i._id_value;
      b[n].is_null = &i._id_null;
      n++;
    }

    // _specieId
    //
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 0;
    b[n].buffer = &i._specieId_value;
    b[n].is_null = &i._specieId_null;
    n++;

    // _namingConventionA
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._namingConventionA_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._namingConventionA_value.capacity ());
    b[n].length = &i._namingConventionA_size;
    b[n].is_null = &i._namingConventionA_null;
    n++;

    // _proteinNameA
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._proteinNameA_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._proteinNameA_value.capacity ());
    b[n].length = &i._proteinNameA_size;
    b[n].is_null = &i._proteinNameA_null;
    n++;

    // _namingConventionB
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._namingConventionB_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._namingConventionB_value.capacity ());
    b[n].length = &i._namingConventionB_size;
    b[n].is_null = &i._namingConventionB_null;
    n++;

    // _proteinNameB
    //
    b[n].buffer_type = MYSQL_TYPE_STRING;
    b[n].buffer = i._proteinNameB_value.data ();
    b[n].buffer_length = static_cast<unsigned long> (
      i._proteinNameB_value.capacity ());
    b[n].length = &i._proteinNameB_size;
    b[n].is_null = &i._proteinNameB_null;
    n++;
  }

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  bind (MYSQL_BIND* b, id_image_type& i)
  {
    std::size_t n (0);
    b[n].buffer_type = MYSQL_TYPE_LONG;
    b[n].is_unsigned = 1;
    b[n].buffer = &i.id_value;
    b[n].is_null = &i.id_null;
  }

  bool access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  init (image_type& i, const object_type& o, mysql::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace mysql;

    bool grew (false);

    // _id
    //
    if (sk == statement_insert)
    {
      unsigned int const& v =
        o._id;

      bool is_null (false);
      mysql::value_traits<
          unsigned int,
          mysql::id_ulong >::set_image (
        i._id_value, is_null, v);
      i._id_null = is_null;
    }

    // _specieId
    //
    {
      int const& v =
        o._specieId;

      bool is_null (false);
      mysql::value_traits<
          int,
          mysql::id_long >::set_image (
        i._specieId_value, is_null, v);
      i._specieId_null = is_null;
    }

    // _namingConventionA
    //
    {
      ::std::string const& v =
        o._namingConventionA;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._namingConventionA_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._namingConventionA_value,
        size,
        is_null,
        v);
      i._namingConventionA_null = is_null;
      i._namingConventionA_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._namingConventionA_value.capacity ());
    }

    // _proteinNameA
    //
    {
      ::std::string const& v =
        o._proteinNameA;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._proteinNameA_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._proteinNameA_value,
        size,
        is_null,
        v);
      i._proteinNameA_null = is_null;
      i._proteinNameA_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._proteinNameA_value.capacity ());
    }

    // _namingConventionB
    //
    {
      ::std::string const& v =
        o._namingConventionB;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._namingConventionB_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._namingConventionB_value,
        size,
        is_null,
        v);
      i._namingConventionB_null = is_null;
      i._namingConventionB_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._namingConventionB_value.capacity ());
    }

    // _proteinNameB
    //
    {
      ::std::string const& v =
        o._proteinNameB;

      bool is_null (false);
      std::size_t size (0);
      std::size_t cap (i._proteinNameB_value.capacity ());
      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_image (
        i._proteinNameB_value,
        size,
        is_null,
        v);
      i._proteinNameB_null = is_null;
      i._proteinNameB_size = static_cast<unsigned long> (size);
      grew = grew || (cap != i._proteinNameB_value.capacity ());
    }

    return grew;
  }

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  init (object_type& o, const image_type& i, database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // _id
    //
    {
      unsigned int& v =
        o._id;

      mysql::value_traits<
          unsigned int,
          mysql::id_ulong >::set_value (
        v,
        i._id_value,
        i._id_null);
    }

    // _specieId
    //
    {
      int& v =
        o._specieId;

      mysql::value_traits<
          int,
          mysql::id_long >::set_value (
        v,
        i._specieId_value,
        i._specieId_null);
    }

    // _namingConventionA
    //
    {
      ::std::string& v =
        o._namingConventionA;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._namingConventionA_value,
        i._namingConventionA_size,
        i._namingConventionA_null);
    }

    // _proteinNameA
    //
    {
      ::std::string& v =
        o._proteinNameA;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._proteinNameA_value,
        i._proteinNameA_size,
        i._proteinNameA_null);
    }

    // _namingConventionB
    //
    {
      ::std::string& v =
        o._namingConventionB;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._namingConventionB_value,
        i._namingConventionB_size,
        i._namingConventionB_null);
    }

    // _proteinNameB
    //
    {
      ::std::string& v =
        o._proteinNameB;

      mysql::value_traits<
          ::std::string,
          mysql::id_string >::set_value (
        v,
        i._proteinNameB_value,
        i._proteinNameB_size,
        i._proteinNameB_null);
    }
  }

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  init (id_image_type& i, const id_type& id)
  {
    {
      bool is_null (false);
      mysql::value_traits<
          unsigned int,
          mysql::id_ulong >::set_image (
        i.id_value, is_null, id);
      i.id_null = is_null;
    }
  }

  struct access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::container_statement_cache_type
  {
    container_statement_cache_type (
      mysql::connection&,
      mysql::binding&)
    {
    }
  };

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::persist_statement[] =
  "INSERT INTO `ProteinNameMap` ("
  "`id`,"
  "`specieId`,"
  "`namingConventionA`,"
  "`proteinNameA`,"
  "`namingConventionB`,"
  "`proteinNameB`)"
  " VALUES (?,?,?,?,?,?)";

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::find_statement[] =
  "SELECT "
  "`ProteinNameMap`.`id`,"
  "`ProteinNameMap`.`specieId`,"
  "`ProteinNameMap`.`namingConventionA`,"
  "`ProteinNameMap`.`proteinNameA`,"
  "`ProteinNameMap`.`namingConventionB`,"
  "`ProteinNameMap`.`proteinNameB`"
  " FROM `ProteinNameMap`"
  " WHERE `ProteinNameMap`.`id`=?";

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::update_statement[] =
  "UPDATE `ProteinNameMap` SET "
  "`specieId`=?,"
  "`namingConventionA`=?,"
  "`proteinNameA`=?,"
  "`namingConventionB`=?,"
  "`proteinNameB`=?"
  " WHERE `id`=?";

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::erase_statement[] =
  "DELETE FROM `ProteinNameMap`"
  " WHERE `id`=?";

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::query_statement[] =
  "SELECT "
  "`ProteinNameMap`.`id`,"
  "`ProteinNameMap`.`specieId`,"
  "`ProteinNameMap`.`namingConventionA`,"
  "`ProteinNameMap`.`proteinNameA`,"
  "`ProteinNameMap`.`namingConventionB`,"
  "`ProteinNameMap`.`proteinNameB`"
  " FROM `ProteinNameMap`"
  " ";

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::erase_query_statement[] =
  "DELETE FROM `ProteinNameMap`"
  " ";

  const char access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::table_name[] =
  "`ProteinNameMap`";

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  persist (database& db, object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    im._id_value = 0;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    obj._id = static_cast< id_type > (st.id ());

    callback (db,
              static_cast<const object_type&> (obj),
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace mysql;

    callback (db, obj, callback_event::pre_update);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, obj._id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (i.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, i);
        sts.id_image_version (i.version);
        idb.version++;
      }

      sts.update_id_image_version (i.version);

      if (!u)
        imb.version++;
    }

    if (sts.update_statement ().execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  erase (database& db, const id_type& id)
  {
    using namespace mysql;

    ODB_POTENTIALLY_UNUSED (db);

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::pointer_type
  access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  find (database& db, const id_type& id)
  {
    using namespace mysql;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj);
      sts.load_delayed ();
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  reload (database& db, object_type& obj)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj._id);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj);
    sts.load_delayed ();
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  find_ (statements_type& sts, const id_type* id)
  {
    using namespace mysql;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());
    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::object_type >
  access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  query (database&, const query_base_type& q)
  {
    using namespace mysql;
    using odb::details::shared;
    using odb::details::shared_ptr;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        query_statement + q.clause (),
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) mysql::object_result_impl<object_type> (
        q, st, sts));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::comppi::entity::ProteinNameMap, id_mysql >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace mysql;

    mysql::connection& conn (
      mysql::transaction::current ().connection ());

    q.init_parameters ();
    delete_statement st (
      conn,
      erase_query_statement + q.clause (),
      q.parameters_binding ());

    return st.execute ();
  }
}

#include <odb/post.hxx>
